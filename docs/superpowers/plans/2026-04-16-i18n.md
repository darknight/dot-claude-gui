# i18n 全量覆盖 — 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 dot-claude-gui 的 UI 全量国际化，支持 zh-CN / en-US / ja-JP 三主力语言，保留 es-ES / fr-FR / ko-KR 扩展位；消除业务组件中所有硬编码文本。

**Architecture:** 自研 `t()` 函数 + 扁平 camelCase 点分 key + JSON 词条表。zh-CN.json 作为 key 全集权威，TS 类型强制主力 3 语完备。appsettings store 增加 `loaded` gating 与脏数据自愈。

**Tech Stack:** Svelte 5 runes、TypeScript 严格模式、Vite（JSON import）、svelte-check。不引入新依赖。

**Spec:** `docs/superpowers/specs/2026-04-16-i18n-design.md`

---

## File Structure

**新增：**
- `src/lib/locales/zh-CN.json` — 简体中文（结构权威）
- `src/lib/locales/en-US.json` — English（兜底）
- `src/lib/locales/ja-JP.json` — 日本語
- `src/lib/locales/es-ES.json` — Español 扩展位（所有 key 值为 `""`）
- `src/lib/locales/fr-FR.json` — Français 扩展位
- `src/lib/locales/ko-KR.json` — 한국어 扩展位

**重写/修改：**
- `src/lib/i18n.ts` — 重写：类型、bundles、`t()`、`detectInitialLocale()`、`localeDisplayName()`、`isSupportedLocale()`
- `src/lib/api/types.ts` — `AppConfig.language` 类型收紧
- `src/lib/stores/appsettings.svelte.ts` — 加 `loaded` 标志、加脏数据自愈
- `src/App.svelte` — 首屏 gating、`document.lang` 同步、13 处导航/占位文本走 `t()`
- `src/lib/components/appsettings/AppSettingsView.svelte` — 8 处硬编码走 `t()`，下拉改用 `ACTIVE_LOCALES`
- `src/lib/components/shared/ScopeSelector.svelte` — 2 处中文 + 若干英文走 `t()`
- `src/lib/components/skills/SkillList.svelte` — "用户技能"、"技能"、残余英文走 `t()`
- `src/lib/components/claudemd/ClaudeMdList.svelte` — "全局"/"项目"、残余英文走 `t()`
- `src/lib/components/claudemd/ClaudeMdEditor.svelte` — "全局"/"项目"、残余英文走 `t()`
- `src/lib/components/memory/MemoryList.svelte` — 补齐残余英文

**不变：**
Rust 侧、Tauri 命令、其他 stores、测试套件（前端仍无单元测试）。

## Verification Commands

本项目前端无单元测试。每个任务用以下两条命令验证：
- **类型/语法：** `pnpm exec svelte-check --tsconfig ./tsconfig.json` — 期望 0 errors。
- **运行时冒烟：** 在特定任务后通过 `pnpm tauri dev` 手动切换语言确认 UI 变化。

---

## Task 1: 搭建 locales 骨架 + i18n.ts 重写

**Files:**
- Create: `src/lib/locales/zh-CN.json`
- Create: `src/lib/locales/en-US.json`
- Create: `src/lib/locales/ja-JP.json`
- Create: `src/lib/locales/es-ES.json`
- Create: `src/lib/locales/fr-FR.json`
- Create: `src/lib/locales/ko-KR.json`
- Rewrite: `src/lib/i18n.ts`

本任务只迁移 `MemoryList.svelte` 已经在用的 2 条 `memory.*` key，保证现有功能不退化；后续任务会持续补齐其他模块的 key。

- [ ] **Step 1: 创建 zh-CN.json**

`src/lib/locales/zh-CN.json`：
```json
{
  "memory.noFilesYet": "当前项目 {name} 还没有生成记忆文件。",
  "memory.dropdownDisabledInProjectScope": "项目 scope 下由顶部 scope 选择器决定，切换到 User Scope 可手动选择。"
}
```

- [ ] **Step 2: 创建 en-US.json**

`src/lib/locales/en-US.json`：
```json
{
  "memory.noFilesYet": "This project {name} has no memory files yet.",
  "memory.dropdownDisabledInProjectScope": "In project scope, memory follows the top-right scope selector. Switch to User Scope to pick manually."
}
```

- [ ] **Step 3: 创建 ja-JP.json**

`src/lib/locales/ja-JP.json`：
```json
{
  "memory.noFilesYet": "このプロジェクト {name} にはまだメモリファイルがありません。",
  "memory.dropdownDisabledInProjectScope": "プロジェクトスコープではメモリは右上のスコープセレクタに従います。手動で選ぶには User Scope に切り替えてください。"
}
```

- [ ] **Step 4: 创建 es-ES.json / fr-FR.json / ko-KR.json（扩展位）**

3 个文件内容相同，所有值为空字符串：
```json
{
  "memory.noFilesYet": "",
  "memory.dropdownDisabledInProjectScope": ""
}
```

- [ ] **Step 5: 重写 i18n.ts**

`src/lib/i18n.ts` 全量替换为：
```ts
import { appSettingsStore } from "$lib/stores/appsettings.svelte";
import zhCN from "./locales/zh-CN.json";
import enUS from "./locales/en-US.json";
import jaJP from "./locales/ja-JP.json";
import esES from "./locales/es-ES.json";
import frFR from "./locales/fr-FR.json";
import koKR from "./locales/ko-KR.json";

export const SUPPORTED_LOCALES = [
  "zh-CN", "en-US", "ja-JP", "es-ES", "fr-FR", "ko-KR",
] as const;
export type Locale = (typeof SUPPORTED_LOCALES)[number];
export const ACTIVE_LOCALES: Locale[] = ["zh-CN", "en-US", "ja-JP"];

export type MessageKey = keyof typeof zhCN;

// 主力三语类型完备；扩展位允许任意子集（值可为空字符串，空字符串会走兜底）。
const bundles: {
  "zh-CN": Record<MessageKey, string>;
  "en-US": Record<MessageKey, string>;
  "ja-JP": Record<MessageKey, string>;
  "es-ES": Partial<Record<MessageKey, string>>;
  "fr-FR": Partial<Record<MessageKey, string>>;
  "ko-KR": Partial<Record<MessageKey, string>>;
} = {
  "zh-CN": zhCN,
  "en-US": enUS,
  "ja-JP": jaJP,
  "es-ES": esES,
  "fr-FR": frFR,
  "ko-KR": koKR,
};

export function t(key: MessageKey, params?: Record<string, string | number>): string {
  const lang = (appSettingsStore.preferences.language as Locale) ?? "en-US";
  let text: string = bundles[lang]?.[key] || bundles["en-US"][key];
  if (!text) {
    if (import.meta.env.DEV) {
      console.warn(`[i18n] missing key: ${key}`);
    }
    return key;
  }
  if (params) {
    for (const [k, v] of Object.entries(params)) {
      text = text.replace(`{${k}}`, String(v));
    }
  }
  return text;
}

export function detectInitialLocale(): Locale {
  const nav = typeof navigator !== "undefined" ? navigator.language : "";
  if (nav.startsWith("zh")) return "zh-CN";
  if (nav.startsWith("ja")) return "ja-JP";
  return "en-US";
}

export function localeDisplayName(loc: Locale): string {
  return {
    "zh-CN": "简体中文",
    "en-US": "English",
    "ja-JP": "日本語",
    "es-ES": "Español",
    "fr-FR": "Français",
    "ko-KR": "한국어",
  }[loc];
}

export function isSupportedLocale(s: unknown): s is Locale {
  return typeof s === "string" && (SUPPORTED_LOCALES as readonly string[]).includes(s);
}
```

- [ ] **Step 6: 运行 svelte-check 验证**

Run: `pnpm exec svelte-check --tsconfig ./tsconfig.json`
Expected: 0 errors, 0 warnings（`MemoryList.svelte` 的 `t("memory.noFilesYet")` / `t("memory.dropdownDisabledInProjectScope")` 调用不会报错，因为 key 是 `MessageKey` 联合的成员）。

- [ ] **Step 7: 提交**

```bash
git add src/lib/locales/ src/lib/i18n.ts
git commit -m "feat(i18n): scaffold locale bundles and rewrite t() with type-safe keys"
```

---

## Task 2: appsettings store + AppConfig 类型改造

**Files:**
- Modify: `src/lib/api/types.ts:358-364`
- Modify: `src/lib/stores/appsettings.svelte.ts`

- [ ] **Step 1: 收紧 AppConfig.language 类型**

`src/lib/api/types.ts` 顶部（第 1 行注释后）新增 import：
```ts
import type { Locale } from "$lib/i18n";
```

将 `AppConfig` 接口改为：
```ts
export interface AppConfig {
  theme: "light" | "dark" | "system";
  language?: Locale;
  fontSize: number;
  sidebarWidth: number;
  subpanelWidth: number;
}
```

（`language` 改为可选，类型收窄为 `Locale` 联合，允许 config.json 里缺失该字段。）

- [ ] **Step 2: 改造 appsettings store**

`src/lib/stores/appsettings.svelte.ts` 全量替换为：
```ts
import { invoke } from "@tauri-apps/api/core";
import type { AppConfig } from "$lib/api/types.js";
import { detectInitialLocale, isSupportedLocale } from "$lib/i18n";

class AppSettingsStore {
  preferences = $state<AppConfig>({
    theme: "system",
    language: undefined,
    fontSize: 14,
    sidebarWidth: 140,
    subpanelWidth: 240,
  });

  loaded = $state(false);

  async load(): Promise<void> {
    try {
      const json = await invoke<string>("read_app_config");
      const saved: Partial<AppConfig> = JSON.parse(json);
      this.preferences = { ...this.preferences, ...saved };
    } catch {
      // Use defaults on error
    }

    // One-time migration from localStorage
    try {
      const legacy = localStorage.getItem("dot-claude-gui-preferences");
      if (legacy) {
        const parsed = JSON.parse(legacy);
        if (parsed.theme) this.preferences.theme = parsed.theme;
        if (parsed.language) this.preferences.language = parsed.language;
        if (parsed.fontSize) this.preferences.fontSize = parsed.fontSize;
        await this.save();
        localStorage.removeItem("dot-claude-gui-preferences");
      }
    } catch {
      // Ignore migration errors
    }

    // Dirty-data self-heal + cold-start detection
    if (!isSupportedLocale(this.preferences.language)) {
      this.preferences.language = detectInitialLocale();
      await this.save();
    }

    this.loaded = true;
  }

  async save(): Promise<void> {
    try {
      await invoke("write_app_config", {
        json: JSON.stringify(this.preferences, null, 2),
      });
    } catch {
      // Silently fail — preferences are not critical
    }
  }

  async update(partial: Partial<AppConfig>): Promise<void> {
    this.preferences = { ...this.preferences, ...partial };
    await this.save();
  }
}

export const appSettingsStore = new AppSettingsStore();
```

- [ ] **Step 3: 运行 svelte-check 验证**

Run: `pnpm exec svelte-check --tsconfig ./tsconfig.json`
Expected: 0 errors。（注意：`AppSettingsView.svelte` 现在 `appSettingsStore.update({ language: (e.target as HTMLSelectElement).value })` 会类型报错，因为 `language` 类型已收紧为 `Locale | undefined`。这在 Task 4 修复。）

**如果 svelte-check 在此步报了上述 `AppSettingsView` 的类型错**，这是预期的，本任务跳过继续；在下一次全量 check 之前（Task 4 末）会修复。**其他任何 error 都不应放行。**

- [ ] **Step 4: 提交**

```bash
git add src/lib/api/types.ts src/lib/stores/appsettings.svelte.ts
git commit -m "feat(i18n): tighten AppConfig.language type and add loaded/self-heal to store"
```

---

## Task 3: App.svelte 首屏 gating + document.lang 同步

**Files:**
- Modify: `src/App.svelte:32-59`（新增 lang effect）
- Modify: `src/App.svelte:190-470`（主体外包 `{#if appSettingsStore.loaded}`）

本任务只动 App.svelte 的结构性包裹，不动任何文本——文本的国际化在 Task 5 一次性做完。

- [ ] **Step 1: 加入 document.lang 同步 effect**

在 `src/App.svelte` 现有 "Font size effect"（第 49 行）之前插入：
```svelte
  // Document lang effect — keeps <html lang> in sync for a11y / font fallback
  $effect(() => {
    const lang = appSettingsStore.preferences.language;
    if (lang) document.documentElement.lang = lang;
  });
```

- [ ] **Step 2: 首屏 gating 外包**

把 `<!-- ===== Root container ===== -->` 起、到 `</div>` （含 `<Toast />`）结束的整个主体（即第 190 行的 `<div class="flex h-screen ...">` 到第 470 行的 `</div>`）用 `{#if appSettingsStore.loaded}...{:else}...{/if}` 包裹，`{:else}` 分支提供空的占位：

```svelte
{#if appSettingsStore.loaded}
  <!-- ===== Root container ===== -->
  <div class="flex h-screen w-screen flex-col overflow-hidden" style="background-color: var(--bg-primary); color: var(--text-primary)">
    <!-- ...既有全部内容不变... -->
  </div>
  <Toast />
{:else}
  <div class="flex h-screen w-screen items-center justify-center" style="background-color: var(--bg-primary)"></div>
{/if}
```

（else 分支故意不放任何文本，避免本身就需要翻译的"加载中"在翻译系统 ready 之前显示。）

- [ ] **Step 3: 运行 svelte-check 验证**

Run: `pnpm exec svelte-check --tsconfig ./tsconfig.json`
Expected: 只保留 Task 2 末的 `AppSettingsView.svelte language 类型` 那一条 error；如有新增错误则退回修正。

- [ ] **Step 4: 手动冒烟**

Run: `pnpm tauri dev`
期望：应用正常启动；`<html>` 的 `lang` 属性反映 `appSettingsStore.preferences.language`（DevTools Elements 面板查看 `<html lang="...">`）；首屏无明显闪烁（主体直到 `loaded === true` 才渲染）。

- [ ] **Step 5: 提交**

```bash
git add src/App.svelte
git commit -m "feat(i18n): add first-paint gating and document.lang sync in App.svelte"
```

---

## Task 4: AppSettingsView 国际化 + appsettings.* 词条

**Files:**
- Modify: `src/lib/locales/*.json` — 新增 `appsettings.*` key
- Rewrite: `src/lib/components/appsettings/AppSettingsView.svelte`

本任务打通"切语言 → UI 翻译"主流程。

- [ ] **Step 1: 在 zh-CN.json 追加 appsettings.* 词条**

在 `src/lib/locales/zh-CN.json` 的对象里（现有 2 条 memory key 后）追加：
```json
  "appsettings.appearance": "外观",
  "appsettings.theme": "主题",
  "appsettings.themeSystem": "跟随系统",
  "appsettings.themeDark": "深色",
  "appsettings.themeLight": "浅色",
  "appsettings.fontSize": "字体大小: {size}px",
  "appsettings.languageLabel": "语言"
```

- [ ] **Step 2: 在 en-US.json 追加同 key**

```json
  "appsettings.appearance": "Appearance",
  "appsettings.theme": "Theme",
  "appsettings.themeSystem": "Follow System",
  "appsettings.themeDark": "Dark",
  "appsettings.themeLight": "Light",
  "appsettings.fontSize": "Font size: {size}px",
  "appsettings.languageLabel": "Language"
```

- [ ] **Step 3: 在 ja-JP.json 追加同 key**

```json
  "appsettings.appearance": "外観",
  "appsettings.theme": "テーマ",
  "appsettings.themeSystem": "システムに従う",
  "appsettings.themeDark": "ダーク",
  "appsettings.themeLight": "ライト",
  "appsettings.fontSize": "フォントサイズ: {size}px",
  "appsettings.languageLabel": "言語"
```

- [ ] **Step 4: 在 es-ES.json / fr-FR.json / ko-KR.json 追加同 key，值为 ""**

3 个扩展位文件都追加：
```json
  "appsettings.appearance": "",
  "appsettings.theme": "",
  "appsettings.themeSystem": "",
  "appsettings.themeDark": "",
  "appsettings.themeLight": "",
  "appsettings.fontSize": "",
  "appsettings.languageLabel": ""
```

- [ ] **Step 5: 重写 AppSettingsView.svelte**

`src/lib/components/appsettings/AppSettingsView.svelte` 全量替换为：
```svelte
<script lang="ts">
  import { appSettingsStore } from "$lib/stores/appsettings.svelte.js";
  import { t, ACTIVE_LOCALES, localeDisplayName, type Locale } from "$lib/i18n";
</script>

<div class="p-6 space-y-8">
  <section class="space-y-4">
    <h2 class="text-lg font-medium" style="color: var(--text-primary)">{t("appsettings.appearance")}</h2>

    <div>
      <label class="block text-sm mb-1" style="color: var(--text-muted)">{t("appsettings.theme")}</label>
      <select
        class="input-base w-auto"
        value={appSettingsStore.preferences.theme}
        onchange={(e) => appSettingsStore.update({ theme: (e.target as HTMLSelectElement).value as "light" | "dark" | "system" })}
      >
        <option value="system">{t("appsettings.themeSystem")}</option>
        <option value="dark">{t("appsettings.themeDark")}</option>
        <option value="light">{t("appsettings.themeLight")}</option>
      </select>
    </div>

    <div>
      <label class="block text-sm mb-1" style="color: var(--text-muted)">{t("appsettings.fontSize", { size: appSettingsStore.preferences.fontSize })}</label>
      <input
        type="range"
        min="12"
        max="20"
        value={appSettingsStore.preferences.fontSize}
        class="w-48"
        oninput={(e) => appSettingsStore.update({ fontSize: parseInt((e.target as HTMLInputElement).value) })}
      />
    </div>

    <div>
      <label class="block text-sm mb-1" style="color: var(--text-muted)">{t("appsettings.languageLabel")}</label>
      <select
        class="input-base w-auto"
        value={appSettingsStore.preferences.language}
        onchange={(e) => appSettingsStore.update({ language: (e.target as HTMLSelectElement).value as Locale })}
      >
        {#each ACTIVE_LOCALES as loc}
          <option value={loc}>{localeDisplayName(loc)}</option>
        {/each}
      </select>
    </div>
  </section>
</div>
```

- [ ] **Step 6: 运行 svelte-check 验证**

Run: `pnpm exec svelte-check --tsconfig ./tsconfig.json`
Expected: 0 errors（Task 2 末遗留的 language 类型错已修复）。

- [ ] **Step 7: 手动冒烟 — 语言切换主流程**

Run: `pnpm tauri dev`
1. 打开 App Settings → 语言下拉。
2. 验证下拉包含 3 项：简体中文 / English / 日本語。
3. 切到 English → "外观"/"主题"等变英文、`<html lang>` 变 `en-US`。
4. 切到 日本語 → 变日文、`<html lang>` 变 `ja-JP`。
5. 关闭 app，重新 `pnpm tauri dev`，选择被保持。

如任一步不符合期望，修到符合再继续。

- [ ] **Step 8: 提交**

```bash
git add src/lib/locales/ src/lib/components/appsettings/AppSettingsView.svelte
git commit -m "feat(i18n): localize AppSettingsView and expose 3-language picker"
```

---

## Task 5: App.svelte 导航与占位文本国际化

**Files:**
- Modify: `src/lib/locales/*.json` — 新增 `nav.*`、`settings.*`、`plugins.*`、`mcp.*` key
- Modify: `src/App.svelte`

- [ ] **Step 1: 在 zh-CN.json 追加词条**

```json
  "nav.settings": "设置",
  "nav.plugins": "插件",
  "nav.skills": "技能",
  "nav.memory": "记忆",
  "nav.commands": "指令",
  "nav.mcp": "MCP",
  "nav.effective": "配置",
  "nav.launcher": "启动",
  "nav.appSettings": "应用设置",
  "nav.expandSidebar": "展开侧边栏",
  "nav.collapseSidebar": "折叠侧边栏",
  "nav.selectItem": "请选择一项查看详情",
  "nav.loadingConfig": "配置加载中...",
  "nav.mergedConfigHint": "当前项目合并后的配置",
  "nav.launcherHint": "选择项目并启动",
  "nav.appSettingsAppearance": "外观",
  "nav.appSettingsConnections": "连接",
  "settings.general": "常规",
  "settings.permissions": "权限",
  "settings.hooks": "Hooks",
  "settings.sandbox": "Sandbox",
  "settings.environment": "环境变量",
  "settings.statusLine": "状态栏",
  "plugins.installed": "已安装",
  "plugins.marketplace": "市场",
  "plugins.manageMarketplaces": "管理市场",
  "plugins.perProject": "按项目",
  "mcp.servers": "服务器",
  "mcp.addServer": "添加服务器"
```

- [ ] **Step 2: 在 en-US.json 追加同 key**

```json
  "nav.settings": "Settings",
  "nav.plugins": "Plugins",
  "nav.skills": "Skills",
  "nav.memory": "Memory",
  "nav.commands": "Commands",
  "nav.mcp": "MCP",
  "nav.effective": "Effective",
  "nav.launcher": "Launcher",
  "nav.appSettings": "App Settings",
  "nav.expandSidebar": "Expand sidebar",
  "nav.collapseSidebar": "Collapse sidebar",
  "nav.selectItem": "Select an item to view details",
  "nav.loadingConfig": "Loading config...",
  "nav.mergedConfigHint": "Merged config for active project",
  "nav.launcherHint": "Select a project and launch",
  "nav.appSettingsAppearance": "Appearance",
  "nav.appSettingsConnections": "Connections",
  "settings.general": "General",
  "settings.permissions": "Permissions",
  "settings.hooks": "Hooks",
  "settings.sandbox": "Sandbox",
  "settings.environment": "Environment",
  "settings.statusLine": "Status Line",
  "plugins.installed": "Installed",
  "plugins.marketplace": "Marketplace",
  "plugins.manageMarketplaces": "Manage Marketplaces",
  "plugins.perProject": "Per-Project",
  "mcp.servers": "Servers",
  "mcp.addServer": "Add Server"
```

- [ ] **Step 3: 在 ja-JP.json 追加同 key**

```json
  "nav.settings": "設定",
  "nav.plugins": "プラグイン",
  "nav.skills": "スキル",
  "nav.memory": "メモリ",
  "nav.commands": "コマンド",
  "nav.mcp": "MCP",
  "nav.effective": "有効設定",
  "nav.launcher": "起動",
  "nav.appSettings": "アプリ設定",
  "nav.expandSidebar": "サイドバーを展開",
  "nav.collapseSidebar": "サイドバーを折りたたむ",
  "nav.selectItem": "項目を選択して詳細を表示",
  "nav.loadingConfig": "設定を読み込み中...",
  "nav.mergedConfigHint": "現在のプロジェクトのマージ済み設定",
  "nav.launcherHint": "プロジェクトを選んで起動",
  "nav.appSettingsAppearance": "外観",
  "nav.appSettingsConnections": "接続",
  "settings.general": "一般",
  "settings.permissions": "権限",
  "settings.hooks": "Hooks",
  "settings.sandbox": "サンドボックス",
  "settings.environment": "環境変数",
  "settings.statusLine": "ステータスライン",
  "plugins.installed": "インストール済み",
  "plugins.marketplace": "マーケットプレイス",
  "plugins.manageMarketplaces": "マーケットプレイス管理",
  "plugins.perProject": "プロジェクト別",
  "mcp.servers": "サーバー",
  "mcp.addServer": "サーバーを追加"
```

- [ ] **Step 4: 在 es-ES / fr-FR / ko-KR.json 追加同 key（值为 ""）**

每个扩展位文件都要追加上面所有 30 条 key，值一律 `""`。示例（其他两个相同结构）：
```json
  "nav.settings": "",
  "nav.plugins": "",
  ...
  "mcp.addServer": ""
```

- [ ] **Step 5: 在 App.svelte 引入 t**

`src/App.svelte` 的 `<script>` 顶部 imports 区域（紧接其他 import 之后）新增：
```ts
  import { t } from "$lib/i18n";
```

- [ ] **Step 6: navButtons 与 appSettingsButton 改为 key**

将 `navButtons` 数组的 `label` 字段从硬编码中文改为 key：
```ts
  const navButtons = [
    { id: "S", labelKey: "nav.settings", icon: "M9.594 3.94..." },
    { id: "P", labelKey: "nav.plugins", icon: "M14.25 6.087..." },
    { id: "K", labelKey: "nav.skills", icon: "m3.75 13.5..." },
    { id: "M", labelKey: "nav.memory", icon: "M19.5 14.25v-2.625..." },
    { id: "D", labelKey: "nav.commands", icon: "M19.5 14.25v-2.625..." },
    { id: "C", labelKey: "nav.mcp", icon: "M5.25 14.25..." },
    { id: "E", labelKey: "nav.effective", icon: "M10.5 6..." },
    { id: "L", labelKey: "nav.launcher", icon: "M15.59 14.37..." },
  ];

  const appSettingsButton = { id: "A", labelKey: "nav.appSettings", icon: "M11.42 15.17..." };
```

（保留原有 icon 字符串，仅把 `label` 改为 `labelKey`；其 `MessageKey` 字符串字面量与 Step 1–3 的 key 一致。）

**TS 强化：** 上述 `labelKey` 需要是 `MessageKey`。为了保持 TS 收窄，在两处都可以用 `satisfies` 语法：
```ts
  const navButtons = [ ... ] satisfies { id: string; labelKey: MessageKey; icon: string }[];
  const appSettingsButton = { id: "A", labelKey: "nav.appSettings", icon: "..." } satisfies { id: string; labelKey: MessageKey; icon: string };
```
并在 import 处加上 `import { t, type MessageKey } from "$lib/i18n";`。

- [ ] **Step 7: 模板里把 `btn.label` 全部换成 `t(btn.labelKey)`**

App.svelte 模板里有两处使用 `btn.label`（`title` 和 `<span>`），均改为 `t(btn.labelKey)`。

例（第 212-227 行那段 `{#each [...navButtons, appSettingsButton] as btn}` 循环）：
```svelte
  {#each [...navButtons, appSettingsButton] as btn}
    <button
      class="flex items-center gap-2 h-9 px-2 rounded-lg text-sm transition-colors overflow-hidden whitespace-nowrap
        {activeNav === btn.id
        ? ''
        : 'hover:text-[var(--text-primary)]'}"
      style="{activeNav === btn.id ? 'background-color: var(--nav-active-bg)' : ''} color: {activeNav === btn.id ? 'var(--nav-active-text)' : 'var(--text-secondary)'}"
      title={t(btn.labelKey)}
      onclick={() => { activeNav = btn.id; }}
    >
      <svg class="w-5 h-5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="1.5">
        <path stroke-linecap="round" stroke-linejoin="round" d={btn.icon} />
      </svg>
      <span class="truncate">{t(btn.labelKey)}</span>
    </button>
  {/each}
```

- [ ] **Step 8: sub-panel 标题里 `navButtons.find(...)?.label` 那一处换掉**

第 266 行 `{navButtons.find((b) => b.id === activeNav)?.label ?? (activeNav === appSettingsButton.id ? appSettingsButton.label : "")}` 改为：
```svelte
  {@const activeBtn = navButtons.find((b) => b.id === activeNav) ?? (activeNav === appSettingsButton.id ? appSettingsButton : null)}
  {activeBtn ? t(activeBtn.labelKey) : ""}
```
或者更简单（保留单行表达式）：
```svelte
  {t(([...navButtons, appSettingsButton].find((b) => b.id === activeNav)?.labelKey) ?? "nav.settings")}
```
（fallback 到 "nav.settings" 只是兜底，正常流里一定会命中一个 btn。）

- [ ] **Step 9: settings/plugins/mcp sub-navigation sections 的 label 改为 key**

```ts
  const settingsSections = [
    { id: "general", labelKey: "settings.general" },
    { id: "permissions", labelKey: "settings.permissions" },
    { id: "hooks", labelKey: "settings.hooks" },
    { id: "sandbox", labelKey: "settings.sandbox" },
    { id: "environment", labelKey: "settings.environment" },
    { id: "statusline", labelKey: "settings.statusLine" },
  ] satisfies { id: string; labelKey: MessageKey }[];

  const pluginsSections = [
    { id: "installed", labelKey: "plugins.installed" },
    { id: "marketplace", labelKey: "plugins.marketplace" },
    { id: "manage-marketplaces", labelKey: "plugins.manageMarketplaces" },
    { id: "per-project", labelKey: "plugins.perProject" },
  ] satisfies { id: string; labelKey: MessageKey }[];

  const mcpSections = [
    { id: "servers", labelKey: "mcp.servers" },
    { id: "add", labelKey: "mcp.addServer" },
  ] satisfies { id: string; labelKey: MessageKey }[];
```

模板里 `{section.label}` 一共 6 处（3 组 × 2 状态分支），全部改为 `{t(section.labelKey)}`。

- [ ] **Step 10: 其他硬编码文本替换**

| 位置 | 原文 | 替换 |
|---|---|---|
| 第 234 行 `title={sidebarCollapsed ? "展开侧边栏" : "折叠侧边栏"}` | 展开侧边栏 / 折叠侧边栏 | `title={sidebarCollapsed ? t("nav.expandSidebar") : t("nav.collapseSidebar")}` |
| 第 357 行 Effective 提示 | Merged config for active project | `{t("nav.mergedConfigHint")}` |
| 第 362 行 Launcher 提示 | Select a project and launch | `{t("nav.launcherHint")}` |
| 第 374、382 行（active/inactive）"外观" | 外观 | `{t("nav.appSettingsAppearance")}` |
| 第 393、401 行（active/inactive）"连接" | 连接 | `{t("nav.appSettingsConnections")}` |
| 第 427 行 `Loading config...` | Loading config... | `{t("nav.loadingConfig")}` |
| 第 462 行 `Select an item to view details` | Select an item to view details | `{t("nav.selectItem")}` |

- [ ] **Step 11: 运行 svelte-check 验证**

Run: `pnpm exec svelte-check --tsconfig ./tsconfig.json`
Expected: 0 errors。（`satisfies` 会强制每个 `labelKey` 是合法的 `MessageKey`。）

- [ ] **Step 12: 手动冒烟 — 导航翻译**

Run: `pnpm tauri dev`
1. 切到 English，验证侧边栏 8 个主按钮 + App Settings 按钮全部变英文；App Settings 下二级导航 "Appearance"/"Connections"；主面板默认提示变英文。
2. 切到 日本語，同样验证。

- [ ] **Step 13: 提交**

```bash
git add src/lib/locales/ src/App.svelte
git commit -m "feat(i18n): localize App.svelte navigation, sub-nav and placeholder text"
```

---

## Task 6: ScopeSelector 国际化 + scope.* 词条

**Files:**
- Modify: `src/lib/locales/*.json` — 新增 `scope.*` key
- Modify: `src/lib/components/shared/ScopeSelector.svelte`

- [ ] **Step 1: 追加 scope.* 词条到 zh-CN.json**

```json
  "scope.user": "用户作用域",
  "scope.projectFallback": "项目",
  "scope.addProject": "添加项目...",
  "scope.selectProjectDir": "选择项目目录"
```

- [ ] **Step 2: 追加到 en-US.json**

```json
  "scope.user": "User Scope",
  "scope.projectFallback": "Project",
  "scope.addProject": "Add project...",
  "scope.selectProjectDir": "Select project directory"
```

- [ ] **Step 3: 追加到 ja-JP.json**

```json
  "scope.user": "ユーザースコープ",
  "scope.projectFallback": "プロジェクト",
  "scope.addProject": "プロジェクトを追加...",
  "scope.selectProjectDir": "プロジェクトディレクトリを選択"
```

- [ ] **Step 4: 追加到 es-ES / fr-FR / ko-KR.json（全部 ""）**

3 个扩展位文件各追加 4 条值为 `""` 的 key。

- [ ] **Step 5: 改造 ScopeSelector.svelte**

在 `<script>` 顶部 imports 后追加：
```ts
  import { t } from "$lib/i18n";
```

将下列位置替换：

| 原行号 / 位置 | 原文 | 替换 |
|---|---|---|
| 第 31 行 `title: "选择项目目录"` | 选择项目目录 | `title: t("scope.selectProjectDir")` |
| 第 49 行 `: "Project"` | Project | `: t("scope.projectFallback")` |
| 第 50 行 `: "User Scope"` | User Scope | `: t("scope.user")` |
| 第 83 行 `<span>User Scope</span>` | User Scope | `<span>{t("scope.user")}</span>` |
| 第 113 行 `<span>添加项目...</span>` | 添加项目... | `<span>{t("scope.addProject")}</span>` |

- [ ] **Step 6: 运行 svelte-check**

Run: `pnpm exec svelte-check --tsconfig ./tsconfig.json`
Expected: 0 errors。

- [ ] **Step 7: 手动冒烟**

Run: `pnpm tauri dev`
1. 点顶栏 scope 选择器 → 验证 "User Scope" / "添加项目..." 在三语下都正确。
2. 切到 English 重试，日文重试。

- [ ] **Step 8: 提交**

```bash
git add src/lib/locales/ src/lib/components/shared/ScopeSelector.svelte
git commit -m "feat(i18n): localize ScopeSelector"
```

---

## Task 7: SkillList 国际化 + skills.* 与 common.loading 词条

**Files:**
- Modify: `src/lib/locales/*.json` — 新增 `skills.*` 与 `common.*` key
- Modify: `src/lib/components/skills/SkillList.svelte`

- [ ] **Step 1: 追加到 zh-CN.json**

```json
  "common.loading": "加载中...",
  "skills.title": "技能",
  "skills.userGroup": "用户技能",
  "skills.noSkills": "未找到技能",
  "skills.invalid": "无效"
```

- [ ] **Step 2: 追加到 en-US.json**

```json
  "common.loading": "Loading...",
  "skills.title": "Skills",
  "skills.userGroup": "User Skills",
  "skills.noSkills": "No skills found",
  "skills.invalid": "Invalid"
```

- [ ] **Step 3: 追加到 ja-JP.json**

```json
  "common.loading": "読み込み中...",
  "skills.title": "スキル",
  "skills.userGroup": "ユーザースキル",
  "skills.noSkills": "スキルが見つかりません",
  "skills.invalid": "無効"
```

- [ ] **Step 4: 追加到 es-ES / fr-FR / ko-KR.json（""）**

每个文件追加 5 条值为 `""` 的 key。

- [ ] **Step 5: 改造 SkillList.svelte**

在 `<script>` 顶部 imports 后追加：
```ts
  import { t } from "$lib/i18n";
```

替换：

| 原行号 / 位置 | 原文 | 替换 |
|---|---|---|
| 第 10 行 `const USER_GROUP = "用户技能";` | "用户技能" | 删除该常量，下面用到的地方直接调 `t("skills.userGroup")` |
| 第 32-36 行 `a === USER_GROUP` 的排序比较 | 使用 USER_GROUP 常量 | 改为 `const userGroup = t("skills.userGroup"); if (a === userGroup) return -1;` 放在 `$derived.by(() => { ... })` 内 |
| 第 54 行 `技能 <span class="normal-case">(...)` | 技能 | `{t("skills.title")} <span class="normal-case">...` |
| 第 69 行 `>Loading...<` | Loading... | `>{t("common.loading")}<` |
| 第 73 行 `>No skills found<` | No skills found | `>{t("skills.noSkills")}<` |
| 第 105、122 行 `title={skill.validationError ?? "Invalid"}` | "Invalid" | `title={skill.validationError ?? t("skills.invalid")}` |

改造后 `pluginNameOf` 和 `groups` 的结构（示例）：
```ts
  function pluginNameOf(skill: SkillInfo): string {
    if (skill.source === "user") return t("skills.userGroup");
    const m = skill.source.match(/^plugin:([^@]+)@/);
    return m?.[1] ?? skill.source;
  }

  const sortedSkills = $derived(
    [...skillsStore.skills].sort((a, b) => {
      const cmp = a.name.localeCompare(b.name);
      return sortBy === "name-asc" ? cmp : -cmp;
    }),
  );

  const groups = $derived.by(() => {
    const userGroup = t("skills.userGroup");
    const map = new Map<string, SkillInfo[]>();
    for (const s of sortedSkills) {
      const k = pluginNameOf(s);
      if (!map.has(k)) map.set(k, []);
      map.get(k)!.push(s);
    }
    return [...map.entries()].sort(([a], [b]) => {
      if (a === userGroup) return -1;
      if (b === userGroup) return 1;
      return a.localeCompare(b);
    });
  });
```

- [ ] **Step 6: 运行 svelte-check**

Run: `pnpm exec svelte-check --tsconfig ./tsconfig.json`
Expected: 0 errors。

- [ ] **Step 7: 手动冒烟**

Run: `pnpm tauri dev`
1. 切到 Skills 面板，验证顶栏 "技能" 在三语下都正确。
2. 验证分组头 "用户技能" 在三语下正确。
3. 如果当前环境无 skills，空状态应显示 "未找到技能" 在三语下正确。

- [ ] **Step 8: 提交**

```bash
git add src/lib/locales/ src/lib/components/skills/SkillList.svelte
git commit -m "feat(i18n): localize SkillList"
```

---

## Task 8: ClaudeMdList + ClaudeMdEditor 国际化 + claudemd.* 与剩余 common.* 词条

**Files:**
- Modify: `src/lib/locales/*.json` — 新增 `claudemd.*` 与扩 `common.*` key
- Modify: `src/lib/components/claudemd/ClaudeMdList.svelte`
- Modify: `src/lib/components/claudemd/ClaudeMdEditor.svelte`

- [ ] **Step 1: 追加到 zh-CN.json**

```json
  "common.save": "保存",
  "common.saving": "保存中...",
  "common.delete": "删除",
  "common.unsaved": "未保存",
  "common.unsavedChanges": "未保存的更改",
  "claudemd.scopeGlobal": "全局",
  "claudemd.scopeProject": "项目",
  "claudemd.confirmDelete": "确认删除此 CLAUDE.md 吗？",
  "claudemd.selectFile": "选择一个 CLAUDE.md 文件进行查看和编辑",
  "claudemd.noFilesForScope": "此作用域下没有 CLAUDE.md 文件",
  "claudemd.groupProject": "项目",
  "claudemd.groupGlobal": "全局",
  "claudemd.createLabel": "CLAUDE.md（待创建）",
  "claudemd.clickToCreate": "点击创建"
```

- [ ] **Step 2: 追加到 en-US.json**

```json
  "common.save": "Save",
  "common.saving": "Saving...",
  "common.delete": "Delete",
  "common.unsaved": "unsaved",
  "common.unsavedChanges": "unsaved changes",
  "claudemd.scopeGlobal": "Global",
  "claudemd.scopeProject": "Project",
  "claudemd.confirmDelete": "Are you sure you want to delete this CLAUDE.md?",
  "claudemd.selectFile": "Select a CLAUDE.md file to view and edit",
  "claudemd.noFilesForScope": "No CLAUDE.md files for this scope",
  "claudemd.groupProject": "Project",
  "claudemd.groupGlobal": "Global",
  "claudemd.createLabel": "CLAUDE.md (create)",
  "claudemd.clickToCreate": "click to create"
```

- [ ] **Step 3: 追加到 ja-JP.json**

```json
  "common.save": "保存",
  "common.saving": "保存中...",
  "common.delete": "削除",
  "common.unsaved": "未保存",
  "common.unsavedChanges": "未保存の変更",
  "claudemd.scopeGlobal": "グローバル",
  "claudemd.scopeProject": "プロジェクト",
  "claudemd.confirmDelete": "この CLAUDE.md を削除してよろしいですか？",
  "claudemd.selectFile": "CLAUDE.md ファイルを選択して表示・編集",
  "claudemd.noFilesForScope": "このスコープに CLAUDE.md ファイルはありません",
  "claudemd.groupProject": "プロジェクト",
  "claudemd.groupGlobal": "グローバル",
  "claudemd.createLabel": "CLAUDE.md（作成）",
  "claudemd.clickToCreate": "クリックして作成"
```

- [ ] **Step 4: 追加到 es-ES / fr-FR / ko-KR.json（""）**

每个文件追加 14 条值为 `""` 的 key。

- [ ] **Step 5: 改造 ClaudeMdEditor.svelte**

`<script>` 顶部 imports 后追加：
```ts
  import { t } from "$lib/i18n";
```

替换：

| 位置 | 原文 | 替换 |
|---|---|---|
| 第 34 行 `confirm("Are you sure you want to delete this CLAUDE.md?")` | 英文字面量 | `confirm(t("claudemd.confirmDelete"))` |
| 第 44 行 `scopeLabel` 函数体 | `scope === "global" ? "全局" : "项目"` | `scope === "global" ? t("claudemd.scopeGlobal") : t("claudemd.scopeProject")` |
| 第 50 行 `Select a CLAUDE.md file to view and edit` | 英文 | `{t("claudemd.selectFile")}` |
| 第 66 行 `unsaved` | 英文 | `{t("common.unsaved")}` |
| 第 79 行 `{claudeMdStore.saving ? "Saving..." : "Save"}` | 英文 | `{claudeMdStore.saving ? t("common.saving") : t("common.save")}` |
| 第 87 行 `Delete` | 英文 | `{t("common.delete")}` |
| 第 96 行 `Loading...` | 英文 | `{t("common.loading")}` |

- [ ] **Step 6: 改造 ClaudeMdList.svelte**

`<script>` 顶部 imports 后追加：
```ts
  import { t } from "$lib/i18n";
```

替换：

| 位置 | 原文 | 替换 |
|---|---|---|
| 第 41 行 `Loading...` | | `{t("common.loading")}` |
| 第 43 行 `No CLAUDE.md files for this scope` | | `{t("claudemd.noFilesForScope")}` |
| 第 47 行 `{isProjectScope ? "Project" : "Global"}` | | `{isProjectScope ? t("claudemd.groupProject") : t("claudemd.groupGlobal")}` |
| 第 68、89、105 行 `CLAUDE.md (create)` | | `t("claudemd.createLabel")` — 注意原文是表达式里 `?:` 分支，三处分别改为 `? "CLAUDE.md" : t("claudemd.createLabel")` 或直接用 `t()` 替换字面量那侧 |
| 第 73、108 行 `click to create` | | `{t("claudemd.clickToCreate")}` |
| 第 76、94 行 `file.scope === "global" ? "全局" : "项目"` | | `file.scope === "global" ? t("claudemd.scopeGlobal") : t("claudemd.scopeProject")` |

- [ ] **Step 7: 运行 svelte-check**

Run: `pnpm exec svelte-check --tsconfig ./tsconfig.json`
Expected: 0 errors。

- [ ] **Step 8: 手动冒烟**

Run: `pnpm tauri dev`
1. 导航到 CLAUDE.md 面板。
2. 切到 English / 日本語 → 列表空态文案、"全局"/"项目"徽章、"Save/Delete/Loading..." 按钮全部翻译正确。
3. 尝试删除一个 CLAUDE.md → 确认对话框的文案也翻译正确。

- [ ] **Step 9: 提交**

```bash
git add src/lib/locales/ src/lib/components/claudemd/
git commit -m "feat(i18n): localize ClaudeMdList and ClaudeMdEditor"
```

---

## Task 9: MemoryList 残余英文国际化 + memory.* / common.* 补齐

**Files:**
- Modify: `src/lib/locales/*.json` — 补充 `memory.*` key
- Modify: `src/lib/components/memory/MemoryList.svelte`

- [ ] **Step 1: 追加到 zh-CN.json**

```json
  "memory.noProjects": "暂无记忆项目",
  "memory.selectProject": "选择项目...",
  "memory.selectProjectAbove": "请在上方选择项目",
  "memory.noFiles": "暂无记忆文件"
```

- [ ] **Step 2: 追加到 en-US.json**

```json
  "memory.noProjects": "No memory projects",
  "memory.selectProject": "Select project...",
  "memory.selectProjectAbove": "Select a project above",
  "memory.noFiles": "No memory files"
```

- [ ] **Step 3: 追加到 ja-JP.json**

```json
  "memory.noProjects": "メモリプロジェクトがありません",
  "memory.selectProject": "プロジェクトを選択...",
  "memory.selectProjectAbove": "上のプロジェクトを選択してください",
  "memory.noFiles": "メモリファイルがありません"
```

- [ ] **Step 4: 追加到 es-ES / fr-FR / ko-KR.json（""）**

每个扩展位文件追加 4 条值为 `""` 的 key。

- [ ] **Step 5: 改造 MemoryList.svelte**

替换：

| 位置 | 原文 | 替换 |
|---|---|---|
| 第 64 行 `No memory projects` | | `{t("memory.noProjects")}` |
| 第 77 行 `Select project...` | | `{t("memory.selectProject")}` |
| 第 88 行 `Loading...` | | `{t("common.loading")}` |
| 第 96 行 `Select a project above` | | `{t("memory.selectProjectAbove")}` |
| 第 98 行 `No memory files` | | `{t("memory.noFiles")}` |
| 第 114 行 `aria-label="unsaved changes"` | | `aria-label={t("common.unsavedChanges")}` |

- [ ] **Step 6: 运行 svelte-check**

Run: `pnpm exec svelte-check --tsconfig ./tsconfig.json`
Expected: 0 errors。

- [ ] **Step 7: 提交**

```bash
git add src/lib/locales/ src/lib/components/memory/MemoryList.svelte
git commit -m "feat(i18n): localize remaining MemoryList strings"
```

---

## Task 10: 残余硬编码扫描与兜底

**Files:**
- 全仓库搜索 + 按需修改

前面任务覆盖了 spec §10 列出的 6 个文件。本任务做一次兜底扫描，抓取其他组件可能遗漏的硬编码英文/中文字面量。

- [ ] **Step 1: 搜索遗留硬编码中文**

Run: `pnpm exec grep -rn '[\u4e00-\u9fff]' src/lib/components src/App.svelte 2>/dev/null | grep -v '\.spec\.' | grep -v locale`
期望：输出应只剩下 SVG path、icon 注释等非 UI 文本。

若仍有 UI 文本中文命中：加到合适的 `*.json` 里（三语 + 3 扩展位），替换源码里字面量为 `t()`。

- [ ] **Step 2: 搜索遗留硬编码英文 UI 文本（常见模式）**

Run: `pnpm exec grep -rnE '"(Loading|Save|Cancel|Delete|Add|Remove|Select|No [A-Z])[^"]*"' src/lib/components src/App.svelte 2>/dev/null | grep -v 'locales/' | grep -v '\.spec\.'`
期望：除了已经本地化的位置，输出应为空或仅余属性值（如 CSS class、aria）。

如有遗漏 UI 文本：在 `common.*` 下找相应 key（或新增），替换为 `t()`。

- [ ] **Step 3: 全量 svelte-check**

Run: `pnpm exec svelte-check --tsconfig ./tsconfig.json`
Expected: 0 errors, 0 warnings。

- [ ] **Step 4: 若有改动则提交**

```bash
git add -A src/lib/components src/lib/locales
git commit -m "feat(i18n): sweep residual hardcoded UI strings"
```

（若本任务无任何改动，跳过 commit。）

---

## Task 11: 手动冒烟验收清单

**Files:** 无（纯验证任务）

按 spec §8 清单逐项执行。若任一项失败，回退到对应前序任务修正并重新走本清单。

- [ ] **Step 1: 冷启动探测（zh）**

```bash
rm -f ~/.dot-claude-gui/config.json
# macOS 将系统语言切到 简体中文（系统偏好设置 → 语言与地区），或临时在启动脚本里设置 LANG=zh_CN.UTF-8
pnpm tauri dev
```
期望：初次打开 UI 即为简体中文，`~/.dot-claude-gui/config.json` 里 `language: "zh-CN"` 已落盘。

- [ ] **Step 2: 冷启动探测（en）**

```bash
rm -f ~/.dot-claude-gui/config.json
# 系统语言切到 English
pnpm tauri dev
```
期望：初次 UI 为英文，落盘 `language: "en-US"`。

- [ ] **Step 3: 冷启动探测（ja）**

```bash
rm -f ~/.dot-claude-gui/config.json
# 系统语言切到 日本語
pnpm tauri dev
```
期望：初次 UI 为日文，落盘 `language: "ja-JP"`。

- [ ] **Step 4: 运行时三语切换**

在 App Settings 依次切 zh-CN / en-US / ja-JP，每切一次：
- 侧边栏 9 个按钮 label 立即变更。
- 导航进 Settings / Plugins / Skills / Memory / CLAUDE.md / MCP / Effective / Launcher / App Settings 每个面板，文案立即变更。
- `<html lang>` 变更（DevTools Elements 面板确认）。
- 无可见闪烁。

- [ ] **Step 5: 脏数据自愈**

```bash
# 在应用关闭状态下
echo '{"theme":"system","language":"xx-YY","fontSize":14,"sidebarWidth":140,"subpanelWidth":240}' > ~/.dot-claude-gui/config.json
pnpm tauri dev
```
期望：启动后 `~/.dot-claude-gui/config.json` 的 `language` 被自愈为 `detectInitialLocale()` 结果，应用 UI 正常。

- [ ] **Step 6: dev warning**

在 dev 环境下，在浏览器 DevTools Console 故意调用：
```js
window.dispatchEvent(new CustomEvent("navigate", { detail: { nav: "A" } }));
```
然后进 Svelte DevTools 或在某组件临时插入 `{t("nonexistent.key" as any)}`（仅作试验），刷新页面 → Console 出现 `[i18n] missing key: nonexistent.key` 警告。验证完删除试验代码。

- [ ] **Step 7: 持久化**

Quit app（Cmd+Q），重新 `pnpm tauri dev`，上次语言选择被保留。

- [ ] **Step 8: 最终确认提交状态**

```bash
git log --oneline -12
git status
```
期望：`git status` clean；git log 显示 Task 1–9 的 commits 已就位。

- [ ] **Step 9: 若有修复，追加 commit**

若 Step 1–7 发现问题并修复，用独立 commit 记录：
```bash
git add -A
git commit -m "fix(i18n): address smoke test findings"
```

---

## Self-Review Notes（内嵌）

- **Spec 覆盖：** spec §4（架构）对应 Task 1；§5.2–5.3 对应 Task 2；§5.5 对应 Task 3；§5.3 对应 Task 4；§5.4 对应 Task 5–9；§7 场景 2（自愈）对应 Task 2 Step 2；§6.1–6.3 的所有数据流场景在 Task 4 Step 7 / Task 11 冒烟验证；§8 测试策略落在 Task 11。
- **无 placeholder：** 每步都包含具体代码、具体命令、具体期望。
- **类型一致性：** `MessageKey` 在 Task 1 定义，所有后续任务用 `satisfies` 收窄以确保 `labelKey` 合法；`Locale` 类型在 Task 1 导出，Task 2 的 AppConfig 引用，Task 4 在 AppSettingsView 的 onchange 里使用。`isSupportedLocale` 在 Task 1 定义，Task 2 使用。
- **增量 TS 错误：** Task 2 Step 3 的 `AppSettingsView` 类型错是刻意遗留、Task 4 一起修的；其他任务都要求 0 errors。
